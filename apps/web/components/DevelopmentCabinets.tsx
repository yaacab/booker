"use client";
import {useState} from "react";
const roles=[{id:"customer",name:"Заказчик",text:"События, команда, переписка и договорённости."},{id:"performer",name:"Исполнитель",text:"Профиль, услуги, календарь и входящие заявки."},{id:"venue",name:"Площадка",text:"Залы, доступность, запросы и события."},{id:"admin",name:"Оператор",text:"Очередь проверки, обращения и сопровождение."}];
export default function DemoCabinets({enabled}:{enabled:boolean}){const [busy,setBusy]=useState(""),[error,setError]=useState("");async function enter(role:string){setBusy(role);setError("");try{const r=await fetch("/dev/session",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({role})});const data=await r.json();if(!r.ok)throw new Error(data.error||"Демо-вход недоступен");sessionStorage.setItem("booker.demo.token",data.token);sessionStorage.setItem("booker.demo.admin",data.is_platform_admin?"1":"0");sessionStorage.removeItem("booker.demo.org");window.location.href=role==="admin"?"/admin":`/cabinet/${role}`}catch(e){setError(e instanceof Error?e.message:"Демо-вход недоступен");setBusy("")}}
return <main className="page-enter"><p className="eyebrow">РЕЖИМ РАЗРАБОТКИ</p><h1>Выберите свой кабинет</h1>{!enabled && <p className="demo-note">Вход без пароля доступен только в закрытом контуре разработки. На публичном сайте используйте обычный вход.</p>}<p className="demo-note">Вход без логина и пароля. Это настоящие интерфейсы с отдельной демонстрационной базой: реальные аккаунты, платежи и бронирования не затрагиваются. Тестовые данные общие для посетителей демо-режима — не вводите личные сведения.</p><div className="entry-paths">{roles.map(r=><article className="assembly-editor" key={r.id}><h2>{r.name}</h2><p>{r.text}</p><button disabled={!enabled||!!busy} onClick={()=>void enter(r.id)}>{busy===r.id?"Открываем…":"Открыть кабинет →"}</button></article>)}</div>{error&&<p role="alert">{error}</p>}</main>}
